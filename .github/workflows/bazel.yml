name: bazel

# Disabled from automatic triggers (push/pull_request/schedule); run manually
# via the Actions tab if needed. `disabled: true` is not valid GitHub Actions
# syntax and was causing every push to this repo to fail workflow validation
# with "Unexpected value 'disabled'".
on:
  workflow_dispatch:

jobs:
  bazel:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, macOS-latest]
      fail-fast: true
      max-parallel: 4

    steps:
    - uses: actions/checkout@v3

    - uses: conda-incubator/setup-miniconda@v2.1.1
      with:
        mamba-version: "*"
        channels: conda-forge
        channel-priority: true
        activate-environment: autodiff
        auto-update-conda: true
        auto-activate-base: false
        show-channel-urls: true

    - name: Configure
      shell: bash -l {0}
      run: |
        conda config --set always_yes yes --set changeps1 no
        conda install conda-devenv bazel
        conda-devenv
        source activate autodiff

    - name: Build
      shell: bash -l {0}
      run: |
        bazel build //examples/forward:all
        bazel build //examples/reverse:all

    - name: Test
      shell: bash -l {0}
      run: |
        bazel test //examples/forward:all --test_output=all
        bazel test //examples/reverse:all --test_output=all
